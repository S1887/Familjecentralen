services:
  - type: web
    name: familjecentralen
    env: docker
    plan: starter
    region: frankfurt
    numInstances: 1
    disk:
      name: family-ops-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DATA_DIR
        value: /data
      - key: PORT
        value: 10000
